# Screen Capture, Flip & Zip (Python)

A simple Python script that:
1. Takes a **screenshot** of the screen
2. Flips the captured image
3. Compresses the final image into a ZIP file

Uses **FFmpeg** under the hood.

---

## 📦 Requirements

- Python 3.8+
- ffmpeg (must be installed and available in PATH)
- zipfile (built-in Python module)

### Install FFmpeg

#### macOS
```bash
brew install ffmpeg
